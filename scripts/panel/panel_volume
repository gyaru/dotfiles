#!/bin/bash
source $XDG_CONFIG_HOME/themes/config

system_volume=$((`pacmd list-sinks | grep -A6 "name: <alsa_output.pci-0000_00_1b.0.analog-stereo>" | grep "volume: " | cut -d'/' -f2 | head -c4`))
output="%{F$COLOR_AA04}$system_volume%%{F-}"
echo $output;


